# Yes-And: accounts-inventory-shell

Original idea: add an Accounts tab where users set up linked accounts.

Yes, and the tab should make each account an inspectable object in the same family as Kwilt mobile inventories: status, purpose, and next action are visible at row level.

Yes, and connection setup should be narrow: add account, sync, see whether it feeds meters, assign missing accounts to lanes later.

Yes, and the Budget tab should stop owning bank setup as a card inside the meter list. The account inventory becomes the durable home for connection health.

Frame recommendation: run the loop with the original frame, positioned as `Accounts as linked-account object inventory`.
