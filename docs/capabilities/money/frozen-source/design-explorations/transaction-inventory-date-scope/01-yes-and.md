# Yes-And: transaction-inventory-date-scope

Yes, and the Transactions tab should become the proof surface for budget trust. The small ask is "show all rows and filter clearly"; the larger product job is "help me believe the meter because I can inspect the evidence."

The expansion should stay bounded. This is not a full finance analytics surface. The useful version is a ledger inventory with an explicit date scope, search/filter affordances, current-month default, and a clear count that distinguishes visible rows from loaded inventory.

Frame recommendation: run the loop with the original frame, positioned as `Transactions as complete inventory with explicit date scope`.
