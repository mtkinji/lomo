# UI contract
Job: set a monthly commitment once and trust its dates.
Authority: user request → native accessibility → Kwilt constitution/tokens/canonical UI → existing Repeat flow. External exemplar: Andrew-provided Outlook Month Days screenshot (2026-09-09). Preserve the same-day/same-week choice and paired ordinal/weekday wheels. Translate to Kwilt drawer, tokens, spacing, and Set action. Reject Outlook-specific styling and a separate navigation stack.
Three-second read: interval, monthly pattern, Set. Primary action: Set. Reveal ordinal/weekday only for Same week each month; retain Same day each month default. Scan: cadence → pattern → selected weekday. No cards, new route, or confirmation.
Reuse: BottomDrawer mechanics; existing RepeatOption selectable rows; native paired wheels; existing interval wheels. Atlas edit/create and small-set choice compositions remain Candidate, used only within this existing edit surface. Native wheels retain the established behavior rather than adopting a new dependency.
States: old date rule, new weekday rule, edit/reopen, switch modes/cadences, cancel, long weekday label, large text. Proof: real Activity detail → Repeat → Custom in current Simulator, save/reopen and recurrence advance. No unresolved product decisions.

Scope: the shared drawer reveals monthly weekday controls only when the calling editor supports them. Chores retains its existing editor because its production server scheduler does not support custom monthly rules.
