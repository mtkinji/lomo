# Converge: Transaction Truth To Five

## Chosen bet

Build Contextual Split Truth inside unified Money transaction detail.

- The normal category picker remains the common path.
- `Split transaction` appears only for posted outflows.
- Two to eight unique category allocations must equal the bank amount exactly.
- The saved receipt lists each category amount while inventory keeps one row.
- A later category assignment or exclusion deletes split rows in the same server transaction.
- Automatic receipt inference, percentages, and recurring split rules remain deferred.

Success signal: a mixed purchase persists across reload/refetch, reconciles exact Summary/category totals, and returns cleanly to one category without residue.
