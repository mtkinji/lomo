# Update

The accepted external connector brief now records:

- originating-surface ownership for MCP interaction;
- no visible Kwilt Chat for external operations;
- completed-only, bounded Home receipts;
- an enforceable future requirement that reviewed external operations have a server-owned inspect/apply/reject continuation;
- the current limitation that many reviewed operations still use mobile-only executors.

No JTBD taxonomy change is needed. The work strengthens existing control, receipt, and recovery steps in `job-flow-nina-trust-ai-with-my-life-system` rather than introducing a new job.
