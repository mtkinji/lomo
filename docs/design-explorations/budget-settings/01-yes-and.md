# Yes-And: Budget Settings

Expansion is intentionally skipped. The request is a bounded ownership correction, not a new configuration platform.

Job elevation: one canonical page can make future global Budget controls discoverable without adding more ellipsis destinations or root Settings rows.

Recommendation: run the loop with the original frame.
