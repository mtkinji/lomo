# Yes-And: Unified Chat operational control plane

Original idea: make correction and action handling dependable through a durable typed control plane.

## Adjacencies

**Yes, and what if it could preserve a typed referent across every retry and correction?**

- Serves: `jtbd-get-help-without-retelling-my-life`
- Job elevation: continuity becomes system state instead of conversational guesswork.
- New value: background, resume, and channel handoff can share the same meaning.
- Cost delta: medium
- Anti-pattern check: pass; no new user-maintained state.

**Yes, and what if every capability declared how its actions resolve targets?**

- Serves: `jtbd-stay-in-control-of-ai-actions`
- Job elevation: “all,” “this,” and “the selected ones” receive domain-owned semantics.
- New value: evidence ranking cannot silently redefine action scope.
- Cost delta: medium
- Anti-pattern check: pass; capability ownership becomes clearer.

**Yes, and what if visible status were compiled from durable outcome facts?**

- Serves: `jtbd-understand-why-ai-suggested-this`
- Job elevation: the user no longer has to reconcile confident prose with cards and receipts.
- New value: model wording cannot claim missing access or successful mutation contrary to the ledger.
- Cost delta: medium
- Anti-pattern check: pass; language becomes calmer and more factual.

**Yes, and what if contradictions became invariant failures rather than analytics curiosities?**

- Serves: `jtbd-trust-this-app-with-my-life`
- Job elevation: trust failures stop the turn before misleading output ships.
- New value: operational errors become discoverable and replayable.
- Cost delta: medium
- Anti-pattern check: pass; no punitive user language.

**Yes, and what if every dogfood incident became a permanent multi-turn release gate?**

- Serves: `jtbd-trust-this-app-with-my-life`
- Job elevation: reliability improves cumulatively instead of through one-off prompt patches.
- New value: model, router, and context changes can be compared against lived failures.
- Cost delta: low
- Anti-pattern check: pass; retain structural fixtures, not intimate values.

## Frame recommendation

Run the loop with the expanded frame: build a reusable operational control plane, with Money category correction as the first complete-inventory proof.
