# Yes-And: Chat Thread Management

This is a bounded refinement, so broader expansion is intentionally skipped. The elevated job is not “more thread administration”; it is preserving trust by making cleanup calm, familiar, and reversible where possible.

Frame recommendation: ship the original request inside the existing Chats list.

