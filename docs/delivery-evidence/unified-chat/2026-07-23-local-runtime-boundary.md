# Unified Chat local runtime boundary — 2026-07-23

## Proven locally

- The isolated `codex/unified-chat-agent-runtime` lane bundled successfully for the booted iPhone 17 Pro simulator from Metro on port 8081.
- The simulator restored an authenticated Kwilt session and loaded the current application bundle.
- With `UNIFIED_CHAT_ENABLED=1` and the local workbench at `http://127.0.0.1:3012/embed/agent-workbench`, the app reached the enabled native Chat surface.
- An exact missing-relation fallback for the unapplied optional `kwilt_agent_client_actions` table allowed existing production threads to hydrate without hiding authorization or other repository failures.
- The v2 native host initialized the shared workbench through its compatibility bridge. The embedded layout kept the composer pinned while the timeline remained independently bounded.
- The OpenAI structured-route schema failure caused by unsupported `uniqueItems` was reproduced against the live proxy, covered by a regression, and fixed while retaining duplicate-capability validation in the parser.
- A fresh authenticated “What should I add to my plan tomorrow?” run completed as `capability_question → plan` with policy reason `day-plan-recommendation` and persisted three pending Plan proposals.
- The visible response used the same capability-owned priority presentation as native Plan: Priority 1 remained first even though it needed a larger opening, and easier calendar placement did not become a competing rank signal.
- The recommendation run persisted Priority 1 as a typed `conversation_referent` event with the exact Activity id, optimistic version, target date, and priority position. The repository hydrated that payload after reload.
- A fresh “Two hours early afternoon” follow-up completed as `capability_action → plan` with policy reason `conversation-follow-up:plan`. It used `plan.read_day_context`, stayed on Priority 1, rejected an unsafe scheduling attempt, and carried the same referent into the clarification turn.
- “One to three PM” then used `plan.reschedule_activity` and created exactly one pending proposal for “Complete the walnut face-frame glue-up and clean the reveals.” The referent cleared after the matching proposal was staged. No proposal was approved and no calendar write occurred.
- Follow-up grounding and visible-text sanitization keep machine object ids tool-only. The new visible response named the Activity and requested time without exposing its internal id or asking for a time zone already represented by Plan context.
- The shared workbench checkout passed 45 Vitest files / 204 tests and a Next.js production build after the v2 command bridge and multiple-proposal timeline changes.

## Unverified boundary

Proposal cards were rendered by the shared timeline path, but no proposal was approved during this proof, so provider creation, reload recovery, and undo remain unproven against the authenticated runtime. The branch contains new pending-client-action, canonical-channel, channel-queue, and server Activity-capture migrations plus new Edge Functions, but none were applied or deployed.

After this simulator session, automated contract work added channel-specific mobile/Phone capability truth and 23 canonical server-staged reviewed-write intents: Profile display-field update; Arc create/update/delete; Goal create/update/delete; Activity update/complete/delete, stable step create/update/complete/delete/reorder, recurrence, reminder, and focus-today; Chapter private-note update; and Plan schedule/reschedule/remove plus grouped chunk scheduling. Profile remains local and authoritative; mobile sync emits only profile id, display name, age range, and optimistic version into an owner-scoped server projection. Phone reads that projection and stages any edit through the existing native Profile proposal, apply, receipt, recovery, and undo path. A coordinator test also proves that an ordinary cross-domain question can call Goal and Activity reads together, return both results to the model, synthesize a plain answer, and record both participating capabilities. The system prompt requires synthesis to preserve capability-owned priority and status. Existing-object tools ground ownership and optimistic version; Goal creation validates an optional parent Arc, Activity reassignment validates the destination Goal owner, step operations address stable ids, and Plan placement validates the configured active provider calendar. The single-proposal service RPC atomically creates each pending proposal, operation, and causal run event. The batch RPC validates 2–10 items and invokes that owner-scoped staging contract inside one database transaction, so mobile either hydrates the complete chunk group or none of it. Native Plan then retains sequential provider apply, one event binding and receipt per chunk, crash recovery, partial-failure reporting, and event-specific undo. The authenticated `agent-run` endpoint and queued worker share the same persistence adapter. Coordinator tests also prove that model-authored “done” wording is replaced by deterministic staged or pending-action truth, including the exact group count. Native Plan and Phone Plan recommendation use one shared priority and eligibility kernel; Phone persists linked-user timezone context to ground relative dates and returns the owned priority order with an explicit no-calendar-placement limitation. No calendar write occurs during the Phone turn. Reminder writes reuse the Activity proposal and defer actual notification scheduling to the native notification service after approval and device-setting checks. Focus-today reuses the soft Plan signal. Focus, Activity location, attachment selection, Activity sharing, Goal sharing, and Plan preference management are separately owner-scoped and stage only the existing native destination; they are not evidence that a timer, permission, attachment, invitation, visibility, availability, or calendar-setting change completed. This is code/test evidence only: the migrations and functions were not deployed, and no real Phone-to-mobile run was claimed.

Automated evidence now also covers the relationship job that the first beta handled only through a narrow Phone post-capture regex. The canonical model can call `relationships.read` or translate explicit facts into `relationships.remember`; validation rejects unknown and unbounded fields, a dedicated Phone permission gates Phone persistence, and one service-only transaction writes existing People/Memory/Event/Cadence records plus applied proposal, operation, receipt, and action-log evidence. Mobile Chat reaches that same executor through one authenticated endpoint using its already-persisted thread, run, and user-message identifiers. `relationships.correct` and `relationships.forget` require a preceding read, exact record id, and matching `updatedAt`; their transaction preserves the same trust records, and native thread hydration now retains the relationship run, applied proposal, and receipt. Exact corrections and individual memory/event/cadence forgetting now carry a restore operation. The existing receipt Undo command sends that receipt to the same authenticated endpoint, where one owner-scoped transaction checks the applied version, restores the exact row, and marks both receipt and proposal undone. Remembering can be compensated by forgetting the exact returned records, but does not yet have one-tap receipt Undo. Whole-person forgetting is absent from both tool catalogs and explicitly excluded from the channel capability manifest until complete dependent-record review and restoration are implemented. A successful typed remember marks legacy enrichment complete for the channel job so retry or compatibility extraction cannot duplicate it. This remains code/test evidence only and is not deployment, simulator, signed-device, or signed-provider proof.

The isolated worktree is not Supabase-linked. A read-only `supabase migration list` from the linked main checkout also could not authenticate the database login role (`401 Unauthorized`; no usable `SUPABASE_DB_PASSWORD`), so remote migration state was not inferred or claimed.

## Next authorized proof

After explicit migration and function-deployment authorization, rerun the signed simulator matrix with the local workbench and prove:

1. “What should I add to my plan tomorrow?” selected apply;
2. grouped chunk review, sequential provider creation, reload recovery, and one-chunk undo;
3. typed cancellation, stale/double apply, native Goal check-in review, background/restore, and exact return;
4. a phone-started queued run continuing in mobile Chat without duplicate work.

This is simulator evidence only. It is not physical-device, signed-provider, deployment, migration, or Phone Agent continuity proof.
