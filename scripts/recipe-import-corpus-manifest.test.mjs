import assert from'node:assert/strict';import test from'node:test';import{RECIPE_IMPORT_CORPUS}from'./fixtures/recipe-import/corpus-manifest.mjs';test('recipe import corpus has 50 rights-safe structural sources across required categories',()=>{assert.equal(RECIPE_IMPORT_CORPUS.length,50);assert.equal(new Set(RECIPE_IMPORT_CORPUS.map((item)=>item.sourceUrl)).size,50);assert.deepEqual([...new Set(RECIPE_IMPORT_CORPUS.map((item)=>item.category))].sort(),['adversarial_page_instruction','fractions_ranges_sections','json_ld','long_family_post','multi_recipe','partial_extraction','paywall_or_error','prose_only'].sort());assert.equal(JSON.stringify(RECIPE_IMPORT_CORPUS).includes('recipeIngredient'),false);});
